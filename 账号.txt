HMJavaEE39@sy.itcast.cn


25562326

mavenË½·þ£ºhttp://localhost:8081/nexus/

SELECT s.* FROM 
			(SELECT s.`name` from 
				(SELECT COUNT(`name`) cn,`name` from student where fs>79 GROUP BY `name`) s,
				(SELECT COUNT(`name`) cu,`name` FROM student GROUP BY `name`) e
			WHERE s.`name` = e.`name` AND s.cn = e.cu) e,student s
WHERE s.`name` = e.`name`;

SELECT s.`name`,s.fs,s.xk 
FROM (SELECT MIN(fs) mi,`name` FROM student GROUP BY `name`) e,student s
WHERE e.`name` = s.`name` AND e.mi>79;

